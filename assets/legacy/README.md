# Legacy assets

Recovered from an earlier Jekyll (chirpy-theme) version of this site whose config/layouts/posts had
already been removed in prior commits, leaving these images orphaned and unreferenced by the site.
Kept here on request, pending a decision on whether/how to use them:

- `logos/` — UNICORN Racing team logo, 4 variants (color/white/UR_Logo/logo3)
- `photos/unicorn_ontrack.jpg`, `photos/unicorn_icra_1.png` — UNICORN Racing car (F1TENTH platform, HMCL)
- `photos/advisor_kwon.png` — advisor photo
- `2022-10th-f1tenth-autonomous-grand-prix/` — 4 photos from a past F1TENTH Grand Prix; the original blog
  post text was already gone by the time these were recovered (removed in an earlier "clean up Jekyll
  files" commit), only the images survived

Nothing here is linked from any page yet, but Jekyll will still copy this folder into the built site
(anything outside `_`-prefixed dirs is copied verbatim), so these ARE reachable at
`/assets/legacy/...` even though unlinked. Not sensitive, just unlinked.

Candidates for later, if wanted: a `_projects/unicorn-racing.md` entry using the logo + on-track photo,
a `_news/` item for the F1TENTH Grand Prix result.
